
<soda>
    <puts text='--------Bug 37255 Started--------'/>
    <csv file="{@global.scriptsdir}/csvs/users.csv" var="user" />
    <script file="{@global.scriptsdir}/modules/lib/login.xml" />

    <!-- Go to password manager -->
    <browser url='{@global.url}?module=Administration&action=index' />
    <link text="Password Management" />
    <button name="edit_generatepasswordtmpl" />

    <!-- Asserts -->
    <browser assert="Insert Variable" />
    <select name="variable_module" exists="true" />
    <select name="variable_name" exists="true" />
    <textfield name="variable_text" exists="true" />

    <puts text='--------Bug 37255 Finished--------'/>
</soda>
