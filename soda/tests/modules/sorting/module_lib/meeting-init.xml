
<soda>
	<puts text="meeting-init.xml starting." />
	<timestamp />
	<link id="moduleTab_Meetings" />
	<csv file="{@global.scriptsdir}/csvs/sorting//meeting_Listview.csv" var="meeting_list_sort">
		<link text="Schedule Meeting" />

		<form id="EditView">
			<textfield id="name" set="{@meeting_list_sort.name}" />
			<textfield id="date_start_date" set="{@meeting_list_sort.date_start_date}" />
			<select id="date_start_hours" set="10" />
			<select id="date_start_minutes" set="30" />
			<select id="parent_type" set="{@meeting_list_sort.parent_type}" />
			<!--account select -->
			<button id="btn_parent_name" />
		</form>
		<textfield id="last_name_advanced" set="{@meeting_list_sort.contact}" /> 	
		<button value="Search" />
		<link text="{@meeting_list_sort.contact}" />
		
		<form id="EditView">
			<button value="Save" />
		</form>
		<wait timeout="2" />
		<button id="edit_button" click="false" />
		<browser assert="{@meeting_list_sort.name}" />
	</csv>

	<puts text="meeting-init.xml finished." />
</soda>

