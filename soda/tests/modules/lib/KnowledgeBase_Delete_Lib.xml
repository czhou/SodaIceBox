
<soda>
    <puts text="Lib: KnowledgeBase_Delete_Lib.xml starting." />
    <link id="moduleTab_KBDocuments" />
    <browser assert="Knowledge Base" />

    <link text="Administer Knowledge Base" />
    <browser assert="Knowledge Base Admin" />

    <select id="action_id" set="Delete Selected Articles" />
    <wait timeout="4" />
    <checkbox id="selected_tags[]" click="true" />
    <wait timeout="4" />
    <checkbox name="massall" click="true" />
    <wait timeout="4" />
    <button id="delete_articles" alert="true" />
    <wait timeout="4" />

    <checkbox id="selected_tags[]" click="true" />
    <wait timeout="4" />
    <span class="pageNumbers" assert="(0 - 0 of 0)" />

    <link id="moduleTab_KBDocuments" />

    <puts text="Lib: KnowledgeBase_Delete_Lib.xml finished." />
</soda>
