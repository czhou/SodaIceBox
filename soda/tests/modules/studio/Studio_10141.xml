<soda>
    
    <!-- check the warning message if you select more fields or less field than function expect -->
    <puts text="Test: Studio_10141.xml starting" />
    <!-- set description field as calculated field and define formula -->
    <timestamp />
    <link text="Admin" />
    <link text="Studio" assertPage="false" />
    <link text="Accounts" class="studiolink" assertPage="false" />
    <link text="Fields" class="studiolink" assertPage="false" />
    <link text="description" class="mbLBLL" assertPage="false" />
    <checkbox id="calculated" set="true" />
    <wait timeout="1" />
    <button value="Edit Formula" />
    <wait timeout="3" />
    <browser assert="Formula Builder" />
    <div id="formulaBuilderWindow" >
      <textfield id="formulaInput" set="contains($billing_address_city,$billing_address_state,$billing_address_street)"/>
      <button value="Save" />
    </div>
    <div id="sugarMsgWindow_c">
    	<browser assert="Invalid Formula" />
    	<browser assert="contains: Requires exactly 2 parameter(s)" />
    	<link text="Close" />
    </div>
    <div id="formulaBuilderWindow" >
      <textfield id="formulaInput" set="add()"/>
      <button value="Save" />
    </div>
    <div id="sugarMsgWindow_c">
    	<browser assert="Invalid Formula" />
    	<browser assert="add: Requires at least one parameter" />
    	<link text="Close" />
    </div>
    <button name="formulacancelbtn" />
    <wait timeout="3" />
    <form id="popup_form_id" >
      <button name="cancelbtn" assertPage="false" />
    </form>
    <wait timeout="3" />
    <browser assert="Add Field" />
    
    <puts text="Test: Studio_10141.xml finished." />
</soda>
