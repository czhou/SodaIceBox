<soda>
    
    <!-- auto updating calculated field that contain rollupAve function when related record changes -->
    <puts text="Test: Studio_10597.xml starting" />
        
     <!-- set description field as calculated field and define formula -->
    <timestamp />
    <link text="Admin" />
    <link text="Studio" assertPage="false" />
    <link text="Accounts" class="studiolink" assertPage="false" />
    <link text="Fields" class="studiolink" assertPage="false" />
    <link text="description" class="mbLBLL" assertPage="false" />
    <checkbox id="calculated" set="true" />
    <wait timeout="1" />
    <button value="Edit Formula" />
    <wait timeout="3" />
    <browser assert="Formula Builder" />
    <div id="formulaBuilderWindow" >
      <textfield id="formulaInput" set="rollupAve($calls,&quot;duration_hours&quot;)"/>
      <button value="Save" />
    </div>
    <wait timeout="3" />
    <form id="popup_form_id" >
      <button name="fsavebtn" assertPage="false" />
    </form>
    <wait timeout="10" />
    <browser assert="Add Field" />
    
    <!--create account record and calls-->
    <script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib_MIN.xml" />       
    <csv file="{@global.scriptsdir}/csvs/studio/Studio_10597.csv" var="studio10597">
   		<script file="{@global.scriptsdir}/modules/lib/Call_Create_Lib.xml" />
   		<button id="edit_button" />
   		<textfield id="duration_hours" set="{@studio10597.durhour1}" />
   		<button id="btn_parent_name" />  	
		<browser assert="Account Search" />
		<link text="{@pre_account_name}"/>
		<button value="Save" />
		<div id="Calls_detailview_tabs">
			<browser assert="{@pre_call_name}" />
			<browser assert="{@pre_account_name}" />
    	</div>    	
    </csv>
    <link id="moduleTab_Accounts" />
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>
	<div id ="Accounts_detailview_tabs">
		<browser assert="{@pre_account_name}" />
	</div>
	<button id="edit_button" />
	<textfield id="description" assert="{@studio10597.ave1}" />
	<button id="SAVE" />
	<table id="detailpanel_1">
		<browser assert="{@pre_account_name}" />
    	<span id="description" assert="{@studio10597.ave1}" />
  	</table>
    
    <!-- change the related field value -->    
	<link id='moduleTab_Calls'/>
	<textfield id="name_basic" set="{@pre_call_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_call_name}" />
	</table>
	<div id="Calls_detailview_tabs">
		<browser assert="{@pre_call_name}" />
		<browser assert="{@pre_account_name}" />
    </div>  
	<button id="edit_button" />
	<textfield id="duration_hours" set="{@studio10597.durhour2}" />
	<button value="Save" />
	<div id="Calls_detailview_tabs">
		<browser assert="{@pre_call_name}" />
		<browser assert="{@pre_account_name}" />
		<browser assert="{@studio10597.durhour2}h" />
    </div>  
    	
	<!-- check the calculation result -->
	<link id="moduleTab_Accounts" />
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>
	<div id ="Accounts_detailview_tabs">
		<browser assert="{@pre_account_name}" />
	</div>
	<button id="edit_button" />
	<textfield id="description" assert="{@studio10597.ave2}" />
	<button id="SAVE" />
	<table id="detailpanel_1">
		<browser assert="{@pre_account_name}" />
    	<span id="description" assert="{@studio10597.ave2}" />
  	</table>
    
    
    <!-- Now uncalculated the field -->
    <link text="Admin" />
    <link text="Studio" assertPage="false" />
    <link text="Accounts" class="studiolink" assertPage="false" />
    <link text="Fields" class="studiolink" assertPage="false" />
    <link text="description" class="mbLBLL" assertPage="false" />
    <checkbox id="calculated" set="false" />
    <form id="popup_form_id" >
      <browser assertnot="Formula Builder" />
      <button name="fsavebtn" assertPage="false" />
    </form>
    <wait timeout="3" />
    
    <!-- Delete account record -->
    <script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
    <script file="{@global.scriptsdir}/modules/lib/Call_Delete_Lib.xml" />
    
    <puts text="Test: Studio_10597.xml finished." />
</soda>
