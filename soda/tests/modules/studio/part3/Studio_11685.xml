<soda>
    
    <!-- auto updating calculated field that contain rollupMin function when related record changed by workflow -->
    <puts text="Test: Studio_11685.xml starting" />
        
     <!-- set description field as calculated field and define formula -->
    <timestamp />
    <link text="Admin" />
    <link text="Studio" assertPage="false" />
    <link text="Accounts" class="studiolink" assertPage="false" />
    <link text="Fields" class="studiolink" assertPage="false" />
    <link text="description" class="mbLBLL" assertPage="false" />
    <checkbox id="calculated" set="true" />
    <wait timeout="1" />
    <button value="Edit Formula" />
    <wait timeout="3" />
    <browser assert="Formula Builder" />
    <div id="formulaBuilderWindow" >
      <textfield id="formulaInput" set="rollupMin($opportunities,&quot;amount&quot;)"/>
      <button id="fomulaSaveButton" />
    </div>
    <wait timeout="3" />
    <form id="popup_form_id" >
      <button name="fsavebtn" assertPage="false" />
    </form>
    <wait timeout="10" />
    <browser assert="Add Field" />
    
    <!--create account record and opps-->
    <script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib_MIN.xml" />       
    <csv file="{@global.scriptsdir}/csvs/studio/Studio_11685.csv" var="studio11685">
   		<script file="{@global.scriptsdir}/modules/lib/Opp_Create_Lib_NoDep.xml" />
    	<button id="edit_button" />
    	<div id="EditView_tabs">
    		<textfield id="amount" set="{@studio11685.amount1}" />
    	</div>
    	<button id="SAVE_HEADER" />
    	<table id="detailpanel_1">
    		<browser assert="{@pre_opp_name}" />
        	<browser assert=" {@studio11685.amount1} " />
      	</table>  	
    </csv>
    <link id="moduleTab_Accounts" />
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>
	<div id ="Accounts_detailview_tabs">
		<browser assert="{@pre_account_name}" />
		<span id="description" assert="{@studio11685.min1}" />
	</div>
    
    <!-- update the related record by workflow-->    
    <div id="globalLinks">
    	<link text="Admin" />
    </div>
    <browser assert="Developer Tools" />
    <link text="Workflow Management" />    
   
	<!--create workflow-->   
	<link text="Create Workflow Definition" />
	<div class="edit view">
		<textfield name="name" set="{@studio11685.wfname} {@stamp}" />
		<select name="base_module" set="{@studio11685.module}" />
	</div>
	<button value="  Save  " />
	<div class="detail view">
		<browser assert="{@studio11685.wfname} {@stamp}" />
	</div>
	<!--create condition-->  
	<button name="New" index="1" />
	<browser assert="Define Condition for Workflow Execution" />
	<radio id="type1" set="true" />
	<button name="save" />
	<table id="contentTable">
		<browser assert="When the target module changes" />
	</table>
	<!--create action-->  
	<button name="New" index="3" />
	<radio id="action_type0" set="true" />
	<button name="Next" />
	<browser assert="Update fields in the target module" />
	<wait timeout="3" />
	<checkbox id="mod_field_7" click="true" />
	<link text="Opportunity Amount" />
	<textfield id="field_7__field_value" set="{@studio11685.amount2}" />
	<button value="Save" />
	<browser assert="Set Opportunity Amount as &apos;{@studio11685.amount2}&apos;" />
	<button value="  Save  " />
	<table id="contentTable">
		<browser assert="Update fields in the target module" />
	</table>
	
	<!--trigger workflow-->  
	<link id='moduleTab_Opportunities'/>
	<textfield id="name_basic" set="{@pre_opp_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_opp_name}" />
	</table>
	<div id="Opportunities_detailview_tabs">
		<browser assert="{@pre_opp_name}" />
	</div>
	<button id="edit_button" />
	<textfield id="description" set="1" /> 
	<button id="SAVE_HEADER" />
	<div id="Opportunities_detailview_tabs">
		<browser assert="{@pre_opp_name}" />
		<browser assert="{@studio11685.amount2}" />
	</div>    	

	<!-- check the calculation result -->
	<link id="moduleTab_Accounts" />
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>
	<div id ="Accounts_detailview_tabs">
		<browser assert="{@pre_account_name}" />
		<span id="description" assert="{@studio11685.min2}" />
	</div>	
	<button id="edit_button" />
	<wait timeout="10" />	
	<textfield id="description" assert="{@studio11685.min2}" />
	<button id="SAVE" />
	<div id="Accounts_detailview_tabs">
		<browser assert="{@pre_account_name}" />
    	<span id="description" assert="{@studio11685.min2}" />
  	</div>    
	
    <!-- Now uncalculated the field -->
    <link text="Admin" />
    <link text="Studio" assertPage="false" />
    <link text="Accounts" class="studiolink" assertPage="false" />
    <link text="Fields" class="studiolink" assertPage="false" />
    <link text="description" class="mbLBLL" assertPage="false" />
    <checkbox id="calculated" set="false" />
    <form id="popup_form_id" >
      <browser assertnot="Formula Builder" />
      <button name="fsavebtn" assertPage="false" />
    </form>
    <wait timeout="3" />
    
    <!-- Delete records -->
    <script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
    <script file="{@global.scriptsdir}/modules/lib/Opp_Delete_Lib.xml" />
    <!--script file="{@global.scriptsdir}/modules/lib/Workflow_Delete_Lib.xml" /-->
    
    <puts text="Test: Studio_11685.xml finished." />
</soda>
