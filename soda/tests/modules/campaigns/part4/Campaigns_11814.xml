
<soda>
	<puts text="Test Case 11814: Verify user defined path of tracker files is saved correctly."/>
	
	<link id="moduleTab_Campaigns"/>
	
	<link text="Set Up Email"/>
	<browser assert="Fill out the form below to modify your system settings so that campaign emails can be sent out."/>
	<textfield id="notify_fromname" set="Sugar_Campaign" />
	<textfield id="notify_fromaddress" set="Sugar_Campaign@sugarcrm.com" />
	<textfield id="mail_smtpserver" set="smtp.gmail.com"/>
	<textfield id="mail_smtpport" set="587"/>
	<checkbox id="mail_smtpauth_req" set="true"/>
	<select id="mail_smtpssl" set="2" />
	<textfield id="mail_smtpuser" set="sugarqa8@gmail.com"/>
	<textfield id="mail_smtpserver" set="smtp.gmail.com"/>
	<textfield id="mail_smtppass" set="sugarcrm1"/>
	<button id="wiz_next_button"/>

	<browser assert="No mail accounts with bounce handling were detected, please create a new one below. ."/>
	<select id="protocol" set="imap"/>
	<checkbox id="ssl" set="true"/>
	<button id="wiz_next_button"/>

	<button id="wiz_submit_button"/>

	<link text="Set Up Email"/>

	<browser assert="Fill out the form below to modify your system settings so that campaign emails can be sent out. "/>
	<textfield id="mail_smtpserver" assert="smtp.gmail.com" />	

	<puts text="Test: Campaigns_11814.xml finished." />
</soda>
