
<soda>
	<puts text="Test Case 307: Account Detail - History sub-panel-Duplicate_Verify that the duplicate activity detail view related to this account is displayed."/>

	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />

	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<wait timeout="5"/>
	<table class="list view">
		<link text="{@pre_account_name}"/> 
	</table>

	<!--Create Meeting record-->
	<div id="LBL_ACCOUNT_INFORMATION">
		<browser assert="{@pre_account_name}"/>
	</div>

	<button id="Activities_schedulemeeting_button"/>
	<textfield id="name" set="Account meeting record"/>
	<select id="status" set="Held"/>
	<button id="Meetings_subpanel_save_button"/>

	<wait timeout="15"/>
	<div id="list_subpanel_history">
		<link text="Account meeting record"/>
	</div>
	<button id="duplicate_button"/>
	<button value="Save"/>

	<div id="Meetings_detailview_tabs">
		<browser assert="Account meeting record"/>
        </div>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Meeting_Delete_Lib.xml"/>
	<puts text="Test: Accounts_0307.xml finished." />
</soda>
