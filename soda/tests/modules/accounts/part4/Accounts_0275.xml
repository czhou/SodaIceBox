
<soda>
	<puts text="Test Case 275: Account Detail - Contracts sub-panel -Verify that error message 'Missing required field: Name' is displayed when editing related 'Contracts' with mandatory fields blank."/>

	<!--Create an account record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />

	<!--Search the record-->
	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<link text="Basic Search" required="false" />
	<form id="search_form">
		<button id="search_form_clear" />
		<textfield id="name_basic" set="{@pre_account_name}" />
		<button id="search_form_submit" />
	</form>

	<table class="list view">
		<browser assert="{@pre_account_name}" />
		<link text="{@pre_account_name}" />
	</table>

	<!--Go to its detail view-->
	<link text="Accounts" click="false" />
	<browser assert="{@pre_account_name}" />

	<!--Create a contract record in product subpanel-->
	<button name="account_contracts_create_button" />
	<button id="Contracts_subpanel_save_button"/>
	<browser assert="Missing required field: Name"/>
	<button id="Contracts_subpanel_cancel_button" alert="true"/>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<puts text="Test: Accounts_0275.xml finsihed." />
</soda>
