
<soda>

	<puts text="Test Case 2866: Select Opportunity -- Project Management_Verify that Opportunity can be selected by clicking Opportunity Name for Project."/>

	<!--Search out the project record created in lib-->
        <link id="moduleTab_Project"/>
	<div class="moduleTitle">
		<link href="index.php?module=Project&action=index" click="false"/>
	</div>

		<textfield id="name_basic" set="{@pre_project_name}"/>
		<button id="search_form_submit"/>
		<link text="{@pre_project_name}"/>
		<browser assert="Resources"/>
		<!--Select Opportunity record-->
		<button id="projects_opportunities_select_button"/>
		<browser assert="Opportunity Search"/>
		
		<textfield id="name_advanced" set="{@pre_opp_name}"/>
		
		<button value="Search"/>
		
		<link text="{@pre_opp_name}"/>
		<div id="list_subpanel_opportunities">
			<link class="listViewTdToolsS1" index="2" click="true" alert="true"/>
			<wait timeout="3"/>
			<link text="{@pre_opp_name}" exist="false" click="false"/>
		</div>

        <puts text="Test Case 2866 is finished!"/>
</soda>
