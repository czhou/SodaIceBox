
<soda>

	<puts text="Test Case 2868: Select Quote -- Project Management_Verify that Quote can be selected by clicking Subject for Project."/>

	<!--Search out the project record created in lib-->
        <link id="moduleTab_Project"/>
	<div class="moduleTitle">
		<link href="index.php?module=Project&action=index" click="false"/>
	</div>

		<textfield id="name_basic" set="{@pre_project_name}"/>
		<button id="search_form_submit"/>
		<link text="{@pre_project_name}"/>
		<browser assert="Resources"/>
		<!--Select Opportunity record-->
		<button id="projects_quotes_select_button"/>
		<browser assert="Quote Search"/>
		
		<textfield id="name_advanced" set="{@pre_Quote_name}"/>

		<button value="Search"/>
		
		<link text="{@pre_Quote_name}"/>
		<div id="list_subpanel_quotes">
			<link class="listViewTdToolsS1" index="2" click="true" alert="true"/>
		</div>
		<wait timeout="5"/>
		<link text="{@pre_Quote_name}" exist="false" click="false"/>

        <puts text="Test Case 2868 is finished!"/>
</soda>
