
<soda>

	<puts text="Test Case 2863: Select Contact -- Project Management_Verify that Contact can be searched when selecting contact for Project."/>

	<!--Search out the project record created in lib-->
        <link id="moduleTab_Project"/>
	<div class="moduleTitle">
		<link href="index.php?module=Project&action=index" click="false"/>
	</div>

		<textfield id="name_basic" set="{@pre_project_name}"/>
		<button id="search_form_submit"/>
		<link text="{@pre_project_name}"/>
		<browser assert="Resources"/>
		<!--Select contact record-->
		<button id="projects_contacts_select_button"/>
		<wait timeout="3"/>
		<browser assert="Contact Search"/>
		
		<button value="Clear"/>
		<textfield id="last_name_advanced" set="{@pre_contact_last_name}"/>
		
		<button value="Search"/>
		
		<wait timeout="10"/>
		<browser assert="{@contact_record.first_name} {@user.name} {@stamp} {@contact_record.last_name}"/>
		<link text="{@contact_record.first_name} {@user.name} {@stamp} {@contact_record.last_name}"/>
		<div id="list_subpanel_contacts">
			<link class="listViewTdToolsS1" index="2" alert="true"/>
		</div>
		<link text="{@contact_record.first_name} {@user.name} {@stamp} {@contact_record.last_name}" exist="false" click="false"/>

        <puts text="Test Case 2863 is finished!"/>
</soda>
