
<soda>
	<!--
		Covert a new lead with copying related meeting to contact
	-->
	<puts text="Test: Leads_11735.xml starting" />

	<!--Goto admin system setting to update coversion selection as copying-->
	<link text="Admin"/>
	<link text="System Settings"/>
	<select name="lead_conv_activity_opt" set="Copy"/>
	<button name="save" />

	<!--Create three records and save its counter-->
	<script file="{@global.scriptsdir}/modules/lib/Lead_Create_Lib.xml" />

	<link id="moduleTab_Leads" />

	<!--Select a lead record-->
	<textfield id="search_name_basic" set="{@pre_lead_first_name}" />
	<button id="search_form_submit" />

	<!--Go to detail view of the first record-->
	<link text="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />
	<browser assert="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />

	<!--Create a meeting from subpanel-->
	<timestamp/>
	<button id="Activities_schedulemeeting_button"/>
	<form id="form_SubpanelQuickCreate_Meetings">
		<csv file="{@global.scriptsdir}/modules/lib/Meeting_Create_Lib.csv" var="lead_11735_meeting">
			<textfield id="name" set="{@lead_11735_meeting.name} {@user.name} {@stamp}" />
			<textfield id="location" set="{@lead_11735_meeting.location}" />
			<textfield id="description" set="{@lead_11735_meeting.description}" />
			<select id="status" set="{@lead_11735_meeting.status}" />
			<textfield id="date_start_date" set="{@lead_11735_meeting.date_start_date}" />
			<select id="date_start_hours" set="{@lead_11735_meeting.date_start_hours}" />
			<select id="date_start_minutes" set="{@lead_11735_meeting.date_start_minutes}" />
			<textfield name="duration_hours" set="{@lead_11735_meeting.duration_hours}" />
			<select name="duration_minutes" set="{@lead_11735_meeting.duration_minutes}" />
			<checkbox name="reminder_checked" set="{@lead_11735_meeting.reminder_checked}" />


			<button id="Meetings_subpanel_save_button" />
			<var var="lead_11735_meeting_name" set="{@lead_11735_meeting.name} {@user.name} {@stamp}" />
		</csv>
	</form>
	<wait timeout="3"/>
	<div id="list_subpanel_activities">
		<link text="{@lead_11735_meeting_name}" click="false" exists="true"/>
	</div>

	<!--Covnert lead-->
	<button name="convert" value="Convert Lead"/>
	<textfield id="Accountsname" set="Leads_11735_Account_{@stamp}"/>
	<select id="lead_conv_ac_op_sel" clear="true"/>
	<select id="lead_conv_ac_op_sel" set="Contact"/>
	<button name="button" value="Save"/>

	<!--Go to newly create contact detail view-->
	<link text="Leads_11735_Account_{@stamp}" click="false" exists="true"/>
	<link text="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}"/>

	<!--Check contact activities subpanel-->
	<div id="list_subpanel_activities">
		<link text="{@lead_11735_meeting_name}" click="false" exists="true"/>
	</div>

	<!--Verify convert lead with meeting record related-->
	<link id="moduleTab_Leads" />
	<textfield id="search_name_basic" set="{@pre_lead_first_name}" />
	<button id="search_form_submit" />

	<!--Go to detail view of the first record-->
	<link text="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />
	<browser assert="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />
	<div id="list_subpanel_activities">
		<link text="{@lead_11735_meeting_name}" click="false" exists="true"/>
	</div>

	<!--Verify convert account without meeting record related-->
	<link id="moduleTab_Accounts" />
	<textfield id="name_basic" set="Leads_11735_Account_{@stamp}" />
	<button id="search_form_submit" />

	<!--Go to account detail view of the first record-->
	<link text="Leads_11735_Account_{@stamp}" />
	<div id="list_subpanel_activities">
		<link text="{@lead_11735_meeting_name}" click="false" exists="false"/>
	</div>

	<!--Goto admin system setting to update coversion selection as copying-->
	<link text="Admin"/>
	<link text="System Settings"/>
	<select name="lead_conv_activity_opt" set="Move"/>
	<button name="save" />

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Lead_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Meeting_Delete_Lib.xml" />
	<puts text="Test: Leads_11735.xml finished" />
</soda>
