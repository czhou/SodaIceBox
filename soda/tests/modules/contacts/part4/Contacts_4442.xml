
<soda>
	<!--
		Mass update_Verify that warning message pops up when clicking "Delete" button in Mass Update sub-panel without selecting any contact.
	-->
	<puts text="Test: Contacts_4442.xml starting." />
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml" />

	<link id="moduleTab_Contacts" />
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
	</div>

	<link id="moduleTab_Contacts" />
	<textfield id="search_name_basic" set="{@pre_contact_first_name} {@pre_contact_last_name}"/>
	<button id="search_form_submit" />


	<!--Back to contact module-->
	<link id="moduleTab_Contacts" alert="true"/>

	<textfield id="search_name_basic" set="{@pre_contact_first_name} {@pre_contact_last_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" click="false" exists="true"/>
		<browser assert="{@pre_contact_first_name} {@pre_contact_last_name}"/>
	</table>

	<link id="actions_link"/>
	<link text="Delete" alert="true"/>
	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" click="false" exists="true"/>
		<browser assert="{@pre_contact_first_name} {@pre_contact_last_name}"/>
	</table>
	

	<!--Clean actions-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<puts text="Test: Contacts_4442.xml finished." />
</soda>
