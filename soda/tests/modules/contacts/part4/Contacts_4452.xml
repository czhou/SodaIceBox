
<soda>
	<!--
		In-line Create project_Verify that warning is displayed when in-line creating a project without the required field filled
	-->
	<puts text="Test: Contacts_4452.xml starting." />
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml"/>

	<!-- *1* Go to Contact module -->
	<link id="moduleTab_Contacts" />
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
	</div>

	<!-- *2* Go to created record detail view-->
	<form id="search_form">
		<button name="clear" />
		<textfield id="search_name_basic" set="{@pre_contact_first_name}" />
		<button id="search_form_submit" />
	</form>

	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</table>
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
		<browser assert="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</div>

	<!-- *3* Go to projects subpanel and try to create project without fillling any field-->
	<button id="projects_contacts_create_button"/>
	<button id="Project_subpanel_save_button"/>
	<div id="form_SubpanelQuickCreate_Project_tabs">
		<browser assert="Missing required field: Name"/>
		<browser assert="Missing required field: Start Date"/>
		<browser assert=" Missing required field: End Date"/>
		
	</div>
	<button id="Project_subpanel_cancel_button" alert="true"/>
	

	<!--Clean actions-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_lib.xml" />
	<puts text="Test: Contacts_4452.xml finished." />
</soda>
