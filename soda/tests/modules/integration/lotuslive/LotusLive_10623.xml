<soda>
	<puts text="Test: LotusLive_10623.xml starting." />

	<link text="{@user.logname}" />
	
	<link text="External Accounts" />
	<button name="EAPM_create_button" />
	<browser assert="Application:" />
	<select id="application" set="LotusLive" />
	<button id="EditViewSave" />	
	<wait timeout="5" />

	<textfield id="username" set="sugarqa3@gmail.com" required="false" timeout="2" />
	<textfield id="password" set="sugar123" required="false" timeout="2" />
	<link id="submit" required="false" timeout="2" />
	
	<wait timeout="5" />
	<link id="allow" required="false" timeout="2" />
	<wait timeout="5" />

	<!-- Verify the warning msg -->
	<browser assert="An account for this application already exists." />
	<browser assert="We have reinstated the existing account." />

	<puts text="Test: LotusLive_10623.xml finished." />
</soda>
